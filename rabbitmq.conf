# RabbitMQ Configuration
listeners.tcp.default = 5672
loopback_users = none  # Allow all users to connect remotely

# Configure management interface to listen for remote connections
management.listener.port = 15672
management.listener.ip   = 0.0.0.0
